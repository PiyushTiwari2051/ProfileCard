{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/profile" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
